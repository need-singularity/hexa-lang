{
  "module": "registry_autodiscover",
  "fn": "registry_filter_excluded",
  "case": "drop_readme_and_ai_md",
  "input": {
    "names": ["alpha", "beta", "README", "gamma", "delta_ai"],
    "exclude": ["README", "delta_ai"]
  },
  "expected": ["alpha", "beta", "gamma"],
  "notes": "Pure list filter — order preserving, O(n*m). The shell-bound registry_scan_dir requires a real filesystem and is host-dependent; not goldened."
}
