{
  "module": "yaml",
  "fn": "yaml_parse_flat",
  "case": "key_value_pairs",
  "input": {
    "text": "name: hexa\nversion: 1.0\nstable: true\n"
  },
  "expected": {
    "name": "hexa",
    "version": "1.0",
    "stable": "true"
  },
  "notes": "yaml_parse_flat is whitespace+colon split; values stay strings (no type coercion)."
}
