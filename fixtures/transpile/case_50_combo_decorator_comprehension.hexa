fn memo(f) {
  let cache = {}
  fn w(x) {
    if x in cache { return cache[x] }
    let v = f(x)
    cache[x] = v
    return v
  }
  return w
}
@memo
fn sq(n) { return n * n }
print([sq(i) for i in [1, 2, 3, 1, 2]])
