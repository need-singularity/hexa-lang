// N1.1x: for v in array (desugar → while)
let xs = [10, 20, 30, 40, 50]
let mut total = 0
for v in xs {
    total = total + v
}
println("sum =", total)

// 문자열 배열
let words = ["hello", "world", "from", "hexa"]
for w in words {
    println("word:", w)
}
println("done")
