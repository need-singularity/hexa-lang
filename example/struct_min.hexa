// N1.1e target: struct 선언/생성/필드 access
struct Point {
    x,
    y
}

fn dist_sq(p) {
    return p.x * p.x + p.y * p.y
}

let a = Point { x: 3, y: 4 }
let b = Point { x: 5, y: 12 }
println("a.x =", a.x, "a.y =", a.y)
println("|a|^2 =", dist_sq(a))
println("|b|^2 =", dist_sq(b))
println("done")
