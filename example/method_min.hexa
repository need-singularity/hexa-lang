// N1.1k: 메서드 호출 — substring, len
let s = "hello world"
println("full:", s)
println("len:", s.len())
println("first:", s.substring(0, 5))
println("last:", s.substring(6, 11))
let tail = s.substring(6, s.len())
println("tail:", tail)
println("done")
