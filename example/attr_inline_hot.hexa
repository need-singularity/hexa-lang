// @inline / @hot / @pure 검증
@inline
fn sq(x) { return x * x }

@hot @pure
fn dist2(a, b) { return sq(a) + sq(b) }

let mut sum = 0
let mut i = 0
while i < 1000000 {
    sum = sum + dist2(i, i + 1)
    i = i + 1
}
println(sum)
println("done")
