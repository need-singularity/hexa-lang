// T49 — fn main() auto-call. Top-level runs first, then main().
// Expected output (both interpreter and VM):
//   init
//   main start
//   42

print("init")

fn helper(x) {
    return x * 6
}

fn main() {
    print("main start")
    print(helper(7))
}
