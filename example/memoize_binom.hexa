// AI-native @memoize 다인자 — Pascal 삼각형 binomial(n,k)
@memoize
fn binom(n, k) {
    if k == 0 { return 1 }
    if k == n { return 1 }
    return binom(n - 1, k - 1) + binom(n - 1, k)
}

println(binom(30, 15))
println(binom(50, 25))
println(binom(60, 30))
println("done")
