// @optimize — 재귀 fn 자동 memoize 주입 검증
// Ackermann(3,10) = 8189 (naive O(2^2^n), memoize 후 instant)
@optimize
fn ack(m, n) {
    if m == 0 { return n + 1 }
    if n == 0 { return ack(m - 1, 1) }
    return ack(m - 1, ack(m, n - 1))
}

println(ack(3, 10))
println(ack(3, 12))
println("done")
