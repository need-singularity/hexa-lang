// Test exec() builtin
let result = exec("echo hello")
println(result)
assert result == "hello"

// Test exec_with_status()
let info = exec_with_status("echo world")
println(info)

// Test exec() with printf (portable)
let text = exec("printf abc")
assert text == "abc"

println("All exec tests passed!")
