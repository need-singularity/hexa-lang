// N1.1l: 파일 I/O + to_string
let path = "build/io_test.txt"
let content = "hexa wrote this\nline 2\nnumber = " + to_string(42)
write_file(path, content)

let got = read_file(path)
println("read back:")
println(got)
println("len =", got.len())
println("done")
