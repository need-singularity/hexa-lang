// bench/probe.hexa — minimal startup probe.
// Measures cold-start overhead: lexer + parser + interpreter init + 1 println.
// Anything beyond a few ms here is pure interpreter startup cost.
println("ok")
