fn main() {
    let s = "hello/world/foo.txt"
    let p = s.last_index_of("/")
    println("last slash at: " + str(p))
    if p >= 0 {
        let base = s.substring(p + 1, len(s))
        println("basename: " + base)
    }
}
