{
  "id": "hexa-ml-commands",
  "description": "ML 로드맵 명령어 — 사용자가 입력하면 해당 절차 실행",
  "ssot": "shared/hexa-lang/ml-next-level.json",
  "commands": {
    "ml": {
      "aliases": ["ml", "ml list"],
      "action": "ml-next-level.json 읽고 전체 항목 ASCII 표 출력",
      "format": "box-drawing table: # | 영역 | 설명 | 영향 | 상태"
    },
    "ml go": {
      "aliases": ["ml go"],
      "action": "status=todo|partial 전부 병렬 bg Agent 발사 (확인 없이 즉시)",
      "rules": ["run_in_background: true", "각 에이전트가 JSON status 갱신", "발사 테이블 출력"]
    },
    "ml next": {
      "aliases": ["ml next"],
      "action": "최우선 항목(P0>P1>P2>P3) 계획 제시 → 사용자 확인 → 구현"
    },
    "ml {id}": {
      "aliases": ["ml 1", "ml 5", "ml 12"],
      "action": "해당 id 항목 구현 시작. existing_files 있으면 읽고 확장, 없으면 self/ml/ 생성"
    },
    "ml done {id}": {
      "aliases": ["ml done 1", "ml done 5"],
      "action": "해당 항목 done 마킹 + JSON summary 재계산"
    },
    "ml add {name} | {desc} | {impact}": {
      "aliases": ["ml add SSM | State Space Model | 긴 문맥"],
      "action": "items 끝에 새 항목 추가 (id=max+1, status=todo)"
    },
    "ml sync": {
      "aliases": ["ml sync"],
      "action": "ml-next-level.json → docs/plans/roadmap.md + README.md ML 표 갱신"
    }
  },
  "implementation_rules": {
    "language": "순수 .hexa (self/ml/ 아래)",
    "rust_modification": false,
    "builtin_registration": "interpreter.hexa + env.hexa + compiler.hexa 3파일 동시 (HX-R-DEC-2)",
    "test_required": "self/test_{feature}.hexa 필수",
    "json_update": "완료 후 ml-next-level.json status/summary 즉시 갱신"
  }
}
