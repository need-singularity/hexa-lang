# .ai-native-algo-baseline — raw #2 grandfather list
# SSOT: self/raws/ai_native_algo.hexa (HX5)
#
# 이 파일에 나열된 .hexa 경로는 raw#2 (naive algorithm) 검사에서 제외된다.
# 목적: new 단계 warn-only 의 false positive 를 점진적으로 제거하면서,
#       동시에 scope 안 새 파일은 strict 적용.
#
# 재생성:
#   hexa scripts/ai_native_algo_lint.hexa --baseline
#
# 원칙 (HX12 패턴):
#   - baseline 은 시간 경과에 따라 줄여간다.
#   - 새 파일 = strict (이 목록에 들어갈 수 없음).
#   - 옛 파일 = 단계적 AI-native rewrite 또는 @allow-naive-algo marker.
#
# 1 path/line. comments (#) 와 blank line 은 무시.
