println("[smoke] start")
use "finetune"
use "finetune_config"
println("[smoke] modules loaded")

let layer = FTLayer {
    w: ft_kaiming_init(4, 3, 42),
    out_dim: 4,
    in_dim: 3
}
println("[smoke] layer created")

let adapter = make_lora_adapter(4, 3, 2, 7)
println("[smoke] adapter created rank=" + str(adapter.rank))

let x = [1.0, 0.5, -0.3]
let y = lora_layer_forward(layer.w, adapter.a, adapter.b, x, 4, 3, 2, 4)
println("[smoke] lora fwd done, y[0]=" + str(y[0]))
println("[smoke] PASS")
