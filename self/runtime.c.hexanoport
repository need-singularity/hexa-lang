Hexa runtime C support layer. SSOT is self/runtime.hexa; this file provides the C runtime (memory, GC, tagged values) that .hexa code compiles against. Cannot be ported to .hexa (it IS the bootstrap runtime).
