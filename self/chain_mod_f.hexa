// self/chain_mod_f.hexa — depth 6 in the use-chain (top of the chain).
// Part of the ROI #149 regression test for 5+ module use-chain silent exit.

use "self/chain_mod_e"

fn chain_fn_f() {
    println("chain_fn_f: depth 6")
    chain_fn_e()
}
