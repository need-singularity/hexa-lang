// run_build.hexa — Self-host compiler entry point
// Usage: ./target/release/hexa ready/self/run_build.hexa <source.hexa> [output]
// Loads pipeline modules then runs hexa_build

use self::lexer
use self::parser
use self::type_checker
use self::codegen_c2

// Forward to hexa_build logic (inlined since use may not chain)
let argv = args()
if len(argv) < 2 {
    println("Usage: hexa ready/self/run_build.hexa <source.hexa> [output]")
    println("Compiles .hexa → C → native binary")
}
