// self/attrs/limit.hexa — singleton + system-state refusal

use "self/attrs/core"

fn meta_limit() -> AttrMeta {
    let mut m = empty_meta()
    m.name = "limit"
    m.category = "safety"
    m.purpose = "singleton + system-state refusal — 동시 실행 N개 제한, 초과 시 refusal"
    m.cost_rule = "load/cpu/gpu 조건 위반 시 즉시 return Void"
    m.example = "@limit(cpu=1, mem_gb=4)\nfn train_epoch() { ... }"
    m.implemented = true
    return m
}

fn check_limit(source: string, lines: [string]) {
    return []
}
