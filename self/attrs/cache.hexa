// self/attrs/cache.hexa — @memoize 의 synonym

use "self/attrs/core"

fn meta_cache() -> AttrMeta {
    let mut m = empty_meta()
    m.name = "cache"
    m.category = "performance"
    m.purpose = "alias for @memoize"
    m.synonym_of = "memoize"
    m.example = "@cache  // == @memoize"
    return m
}

fn check_cache(source: string, lines: [string]) {
    return []
}
