let A = tensor_zeros(4)
let B = tensor_zeros(4)

let _ = mat_add_inplace(A, B)
println("mat_add_inplace OK")

let C = axpy(A, B, -0.001)
println("axpy OK len=" + str(len(C)))

let D = mat_scale_inplace(A, 2.0)
println("mat_scale_inplace OK len=" + str(len(D)))

// VM tier 테스트 — 동일 빌트인 동등성
