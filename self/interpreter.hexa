// === FLATTENED by tool/flatten_imports.hexa ===
// target: self/codegen_c2.hexa
// files:  // === FLATTENED by tool/flatten_imports.hexa ===
// target: self/codegen_c2.hexa
// files:
// order:  reverse-topo (root-first, leaves-last)
//
// ── fn_attrs bitmask (canonical reference) ─────────────────────────
// Populated by fn_attrs_set (self/hexa_full.hexa:5511); queried by
// fn_attr_bitmask / has_lazy_attr / has_specialize_attr.
//
//   bit 0  (1)    memo / memoize / cache
//   bit 1  (2)    lazy            ← thunk-cache single-result route
//   bit 2  (4)    specialize
//   bit 3  (8)    pure
//   bit 4  (16)   stable
//   bit 5  (32)   track
//   bit 6  (64)   deprecated
//   bit 7  (128)  close
//   bit 8  (256)  limit
//   bit 9  (512)  retry
//   bit 10 (1024) fallback
//   bit 11 (2048) guard
//
// @lazy dispatch (self/hexa_full.hexa:8965-8971, 9271-9274):
//   pre-body:  if lazy_lookup(name) hit → return cached val
//   post-body: lazy_store(name, result) — 64-bucket hash over fn name
//
// Phase-1 static guard: check_lazy (self/attrs/lazy.hexa) rejects
// @lazy + @ossify and @lazy on fns with params (arg-invariant assumption
// violates cached-single-result semantics).
