// self/poc/expr/null_lit.hexa
// @kinds NullLit
//
// Phase 0 demo stub — proves filename=dispatch-key workflow.
//
// Rule: filename `null_lit.hexa` under `expr/` → fn `poc_ex_null_lit`.
// Annotation `@kinds NullLit` declares which AST kind(s) this handles.
// Parser may not emit NullLit yet; this handler is dead code but
// registers in the generated dispatcher to validate the pipeline.

use "poc/common"
use "mini_native"

fn poc_ex_null_lit(e) -> int {
    println("[poc] NullLit not yet implemented")
    return -1
}
