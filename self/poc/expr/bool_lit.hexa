// self/poc/expr/bool_lit.hexa
// @kinds BoolLit

use "poc/common"
use "mini_native"

fn poc_ex_bool_lit(e) -> int {
    let v = poc_fresh_vid()
    let b = if e.value == "true" { 1 } else { 0 }
    poc_emit(mn_load_imm(v, b))
    return v
}
