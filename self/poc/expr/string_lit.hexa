// self/poc/expr/string_lit.hexa
// @kinds StringLit

use "poc/common"
use "mini_native"

fn poc_ex_string_lit(e) -> int {
    let str_id = poc_intern_str(e.value)
    let vid = poc_fresh_vid()
    poc_emit(mn_str_addr(vid, str_id))
    return vid
}
