// self/poc/expr/tuple_lit.hexa
// @kinds TupleLit, Tuple

use "poc/common"
use "mini_native"

fn poc_ex_tuple_lit(e) -> int {
    print("[poc] TupleLit not yet implemented: " + str(len(e.items)))
    return -1
}
