// self/poc/builtin/exit.hexa
// @name exit

use "poc/common"
use "mini_native"

fn poc_bi_exit(e) -> int {
    let code_vid = poc_lower_expr(e.args[0])
    if code_vid < 0 { return -1 }
    poc_emit(mn_syscall1(33554433, code_vid))
    return code_vid
}
