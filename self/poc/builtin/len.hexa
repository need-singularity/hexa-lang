// self/poc/builtin/len.hexa
// @name len
// @arity 1

use "poc/common"
use "mini_native"

fn poc_bi_len(e) -> int {
    let base_vid = poc_lower_expr(e.args[0])
    if base_vid < 0 { return -1 }
    let vid = poc_fresh_vid()
    poc_emit(mn_load_off(vid, base_vid, 0))
    return vid
}
