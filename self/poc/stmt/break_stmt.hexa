// self/poc/stmt/break_stmt.hexa
// @kinds BreakStmt

use "poc/common"
use "mini_native"

fn poc_st_break_stmt(stmt) -> int {
    let bl = poc_top_break_label()
    if bl < 0 { println("[poc] break outside loop"); return -1 }
    poc_emit(mn_jmp(bl))
    return 0
}
