// self/poc/stmt/return.hexa
// @kinds ReturnStmt

use "poc/common"
use "mini_native"

fn poc_st_return(stmt) -> int {
    let rv = poc_lower_expr(stmt.left)
    if rv < 0 { return -1 }
    poc_emit(mn_ret_ep(rv))
    return 1
}
