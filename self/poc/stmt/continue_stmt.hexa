// self/poc/stmt/continue_stmt.hexa
// @kinds ContinueStmt

use "poc/common"
use "mini_native"

fn poc_st_continue_stmt(stmt) -> int {
    let cl = poc_top_cont_label()
    if cl < 0 { println("[poc] continue outside loop"); return -1 }
    poc_emit(mn_jmp(cl))
    return 0
}
