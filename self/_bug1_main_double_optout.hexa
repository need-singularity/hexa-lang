// Negative-test sibling for _bug1_main_double.hexa.
// With @manual_main attr, the parser MUST allow the (fn main + explicit main()) pattern.
// Expected: prints "X" twice (intentional explicit call) without parser hard-fail.
@manual_main
fn main() -> void {
    println("X")
}
main()
