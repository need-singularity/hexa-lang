// Negative-test for silent-failure-enforcement Class 3.
// A `#!hexa` shebang appearing on line >1 should hard-fail with a clear
// diagnostic (not the previous cryptic parse error from `!hexa` token leak).
println("before")
#!hexa strict
println("after")
