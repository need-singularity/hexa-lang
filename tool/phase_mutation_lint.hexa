// tool/phase_mutation_lint.hexa — raw#22 enforcer (infra-only-phase-immutable)
//
// rule: raw:infra-only-phase-immutable — blowup 9-phase 파이프라인 의미론 흐름 고정
// (field→holo→quantum→string→toe→absolute). infra-only 개선만 허용, phase 로직/discovery/seed 진화 변경 금지.
//
// follow-up: phase AST diff + infra-only 판정 (refactor vs semantic)
//
// stub — implementation pending
