// tool/atlas_append_check.hexa — raw#23 enforcer (schema-guard-mandatory)
//
// rule: raw:schema-guard-mandatory — 외부 source append (atlas.n6, permissions_ssot.json 등) 시
// schema guard 통과 필수. 무효 라인 reject. _guarded_append_atlas() 사용 강제.
// shared/blowup/lib/atlas_guard.hexa.inc 공유 헬퍼로 format 검증.
//
// follow-up: call-graph 에서 atlas append 지점 스캔 + _guarded_append_atlas 호출 여부 확인
//
// stub — implementation pending
