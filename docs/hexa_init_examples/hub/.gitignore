# hexa build artifacts
build/
target/

# hexa sealed sidecars (허브는 .sealed.hash 를 git 에 포함)
# *.sealed.hash 는 commit 함 — 봉인 이력 공유

# OS
.DS_Store
Thumbs.db
._*

# editor / IDE
.vscode/
.idea/
*.swp

# 허브 로컬 캐시
shared/cache/
shared/runtime/

# local env / secrets
.env
.env.local
