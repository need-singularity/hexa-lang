fn fib(n: int) -> int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

println("Fibonacci sequence (first sigma(6)=12 terms):")
for i in 0..12 {
    println("  fib(", i, ") =", fib(i))
}
