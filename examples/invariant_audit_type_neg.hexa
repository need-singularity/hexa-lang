// examples/invariant_audit_type_neg.hexa — audit_type negative case
//
//   ./hexa check examples/invariant_audit_type_neg.hexa
//     → [audit_type] 'RareType' found 0 reference(s) + 0 violation + exit 0
//
// audit_type 은 절대 차단하지 않음 — 0 references 여도 위반 0.

@invariant {
    id: "I5",
    name: "raretype_audit",
    predicate: "audit_type",
    pattern: { target: "RareType", context: "" },
    exempt: []
}

fn unrelated() {
    return 42
}

unrelated()
