// @simd 검증 — 벡터화 힌트 주입
@simd
fn sum_squares(n) {
    let mut s = 0
    let mut i = 0
    while i < n {
        s = s + i * i
        i = i + 1
    }
    return s
}

println(sum_squares(10000000))
println("done")
