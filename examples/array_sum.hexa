// N1.1d target: 배열 리터럴, 인덱싱, len, for-in
fn main_demo() {
    let xs = [1, 2, 3, 4, 5, 6, 10]
    let mut total = 0
    let mut i = 0
    while i < len(xs) {
        total = total + xs[i]
        i = i + 1
    }
    println("sum =", total)
    return 0
}

let _ = main_demo()
println("done")
