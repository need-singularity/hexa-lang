<!-- @own(
       sections = [WHY, SYNTAX, PARSER],
       strict   = true,
       order    = sequential
     ) -->

# own_selfhost — PASS fixture

## WHY
멀티라인 선언 + strict + sequential 전부 PASS.

## SYNTAX
@own(...) 멀티라인 허용 증명.

## PARSER
state machine 이 이 파일을 통과.
