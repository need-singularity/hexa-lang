// N1.1c target: fn 선언, 매개변수, return, 사용자 함수 호출
fn fact(n) {
    if n <= 1 {
        return 1
    }
    return n * fact(n - 1)
}

fn sum(a, b) {
    return a + b
}

println("fact(5) =", fact(5))
println("fact(10) =", fact(10))
println("sum(3,4) =", sum(3, 4))
println("done")
