// t62b — @lazy + @ossify contradiction fixture
// check_lazy (self/attrs/lazy.hexa) must emit an error diagnostic.
// This file is a STATIC fixture, not meant to run — it tests the lint layer.

@ossify
@lazy
pub fn bad_sealed() {
    return 1
}
