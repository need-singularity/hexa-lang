@test fn t_add() {
    assert(1 + 1 == 2)
}

@test @skip fn t_pending() {
    assert(false)
}

@test fn t_string() {
    assert("hello".starts_with("hel"))
}

@bench fn b_loop() {
    let mut s = 0
    let mut i = 0
    while i < 1000 {
        s = s + i
        i = i + 1
    }
}
