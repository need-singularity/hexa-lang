// PASS fixture — proper exec_with_status() usage with int comparison.
// Lint MUST NOT fire on this file.
let result = exec_with_status("ls")
if result[1] == 0 {
    println("ok")
}
let r2 = exec_with_status("test -d /tmp")
if r2[1] != 0 {
    println("missing")
}
