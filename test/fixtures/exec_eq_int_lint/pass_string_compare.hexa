// PASS fixture — exec() compared to a STRING literal (correct usage).
// Lint MUST NOT fire on this file.
if exec("uname 2>/dev/null | tr -d ' \\n\\t'") == "Linux" {
    println("linux")
}
let os = exec("uname").trim()
if os == "Darwin" {
    println("mac")
}
