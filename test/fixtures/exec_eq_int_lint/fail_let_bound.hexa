// FAIL fixture — single-hop let binding: `let rc = exec(...); if rc == 0`.
// Lint MUST fire on line 5.
fn run() {
    let rc = exec("some-cmd")
    if rc == 0 {
        println("never reached")
    }
}
