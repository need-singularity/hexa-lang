// t54 — L1 scope detection (NO strict header on purpose)
// Without `#!hexa strict`, L2/L3 violations become warnings (not blocking).

fn warn_only(n) {
    if n > 999 { return 1 }      // L2 warning (no strict header)
    return 0
}

fn main() {
    println(warn_only(50))
}
