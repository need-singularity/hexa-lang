// t62c — @lazy on fn with params (Phase-1 restriction) fixture
// check_lazy (self/attrs/lazy.hexa) must emit an error diagnostic
// ("Phase-1 supports only zero-arg fns").
// Static fixture, not meant to run.

@lazy
fn bad_arg(x) {
    return x + 1
}
