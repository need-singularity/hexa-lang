// tests/regression/anima_bug3/mod_c.hexa
//
// NOTE: this module defines its own `fn main()` as a test/selftest entry.
// This mirrors the real-world anima pattern where shared helpers ship a
// module-level `main()` for ad-hoc testing, while the driver also defines
// its own `fn main()`. flatten_imports must NEVER mangle `main` — the C
// entry point — under any collision condition.

fn mod_c_hi() {
    return 100
}

// Secondary test entry (module selftest). Must NOT collide+mangle the
// driver's main in flatten output.
fn main() {
    println("mod_c self-test")
}
