// tests/regression/anima_p0_bug3/driver.hexa
//
// Regression: silent-exit at 3+ module `use` chain.
//
// Repro: `hexa build tests/regression/anima_p0_bug3/driver.hexa -o /tmp/p0bug3`
// Expected: the produced binary prints "hello from driver" and returns 0.
// BUG: build "OK", binary exits 0 silently, build/artifacts/driver.c main() body empty.
//
// Graph: driver → mod_a → mod_b → mod_c (3-deep transitive chain).
use "mod_a"

fn main() {
    println("hello from driver " + str(mod_a_hi()))
}
